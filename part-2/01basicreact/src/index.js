import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
// react create a virtual dom and compare and change the main dom


root.render(
    <App />
);